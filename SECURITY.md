# 安全策略

## 支持范围

当前 `main` 分支接受安全修复。该项目是用于学习和作品展示的本地部署 MVP，不承诺生产级安全支持。

## 报告安全问题

请不要在公开 Issue 中发布 API Key、密码、真实企业文档、可利用的漏洞细节或其他敏感信息。请通过 GitHub 仓库所有者的私密联系方式报告，并说明：

- 受影响的版本或提交；
- 复现步骤和影响范围；
- 已采取的临时缓解措施（如有）。

收到报告后，将先确认影响范围，再以最小改动修复并补充回归测试。

## 部署提醒

- 公网部署必须启用 HTTPS，并设置 `COOKIE_SECURE=true`。
- 必须替换演示账号和默认数据库密码，密钥应由部署平台的 Secret 管理能力注入。
- 上传文档属于不可信输入；生产环境还需增加恶意文件扫描、资源限额、审计和备份策略。
